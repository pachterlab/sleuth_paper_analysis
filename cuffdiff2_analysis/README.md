# Perform kallisto-sleuth analysis of the RNA-Seq data from the Trapnell et. al. 2013 "Cuffdiff2 paper"

To initialize:

```{sh}
snakemake metadata
```

Then:

```{sh}
snakemake -p
```

will run everything
